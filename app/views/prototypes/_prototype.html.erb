<div class="card">
  <%= link_to image_tag(prototype.image, class: :card__img), prototype_path(prototype.id), method: :get%>
  <div class="card__body">
    <%= link_to prototype.title, root_path, class: :card__title%>
    <p class="card__summary">
      <%= prototype.catch_copy %>
    </p>
    <%= link_to prototype.user.name, prototype_path(prototype.id), method: :get, class: :card__user %>
  </div>
</div>
<%# image_tag()で囲って表示すること
link_toで表示したいのはprototypeのカラムのタイトル、キャッチコピー、であること
current_user.nameではなくprototypeのカラムに結びついている名前なので
prototype.user.nameであること
rails routesで調べたときにURIパターンがある場合はpath(同じprifix名.id)をつけること %>

